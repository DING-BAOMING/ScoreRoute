/app/logs/*.log {
    daily
    rotate 7
    missingok
    notifempty
    compress
    delaycompress
    create 644 root root
    sharedscripts
    postrotate
        if [ -f /app/gateway.pid ]; then
            kill -USR1 $(cat /app/gateway.pid) 2>/dev/null || true
        fi
    endscript
}
